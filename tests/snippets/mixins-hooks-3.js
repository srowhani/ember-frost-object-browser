function serializeQueryParams (queryObject) {
  // context --> controller layer/controller
  // queryObject --> the query params in current context/route
  return queryObject
}

function willQuery (params) {
  // context --> controller layer/controller
  // queryObject --> the query params in current context/route
}

function didQuery () {
  // context --> controller layer/controller
  // queryObject --> the query params in current context/route
}

function didReceiveResponse (response) {
  // context --> controller layer/controller
  // response --> response received from server
  return response
}
